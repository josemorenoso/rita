import Lupa from "@/components/lupa/Lupa";

/**
 * La mesa de trabajo de Ruth Alcaraz (fin-05, Control de Costes). Como el
 * resto de pantallas, la página es un envoltorio: toda la lógica vive en el
 * componente para poder moverla de ruta sin tocar nada más.
 */
export default function Page() {
  return <Lupa />;
}
