import RoutePlanner from "@/components/RoutePlanner";

/**
 * La mesa de despacho de Kai (log-01). Como la portada, la página es un
 * server component de una línea y toda la interactividad vive en el componente.
 */
export default function Page() {
  return <RoutePlanner />;
}
