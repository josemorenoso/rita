import LeadFinder from "@/components/LeadFinder";

/**
 * La portada es la herramienta. La simulación AIOS vive en /oficina.
 * Para intercambiarlas basta con cambiar qué componente devuelve cada página.
 */
export default function Page() {
  return <LeadFinder />;
}
